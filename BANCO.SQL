/*CREATE DATABASE ESCOLA; */
/*DROP DATABASE ALUNOS*/
/*DROP TABLE ALUNOS*/
/*CREATE TABLE ALUNOS(
 ID INT PRIMARY KEY AUTO_INCREMENT,
 NOME VARCHAR(120) NOT NULL 
);*/

/* SELECT ID * FROM ALUNOS */

SELECT ID, NOME FROM ALUNOS LIMIT 10

INSERT INTO ALUNOS (NOME)
VALUES ('JANSEN'),('VITOR'),('PEDRO')

/*
SELECT NOME FROM ALUNOS WHERE ID = 1;
*/
/*
SELECT ID, NOME FROM ALUNOS ORDER BY NOME ASC;
*/
/*SELECT ID, NOME FROM ALUNOS ORDER BY NOME DESC;*/
/*FSFEESF*/
/*
SELECT COUNT(*) AS TOTAL_ALUNOS FROM ALUNOS;
*/
/*
SELECT * FROM ALUNOS WHERE NOME LIKE "J%"


SELECT * FROM alunos WHERE nome LIKE "%O"

INSERT INTO alunos(nome)
VALUES ('JANSEN')

SELECT DISTINCT nome FROM alunos;
*/

SELECT nome, COUNT(*) AS TOTAL FROM alunos GROUP BY nome ORDER TOTAL DESC;
/*
CREATE TABLE CURSOS(
	ID INT AUTO_INCREMENT PRIMARY KEY,
	NOME_CURSO VARCHAR(120) NOT NULL
);*/

CREATE TABLE MATRICULAS(
	ID INT AUTO_INCREMENT PRIMARY KEY, 
	ALUNO_ID INT, 
	CURSO_ID INT,

	FOREIGN KEY(ALUNO_ID	) REFERENCES ALUNOS(ID),
	FOREIGN KEY(CURSO_ID	) REFERENCES CURSOS(ID)
) ;

INSERT INTO cursos (nome_curso) VALUES 
('Informatica'),
('Aadministração'),
('Meacânica'),
('Eletrotécnica');

INSERT INTO matriculas (ALUNO_ID, CURSO_ID) VALUES
(1,1),
(2,1),
(3,2),
(1,2);

SELECT * FROM MATRICULAS
INNER JOIN alunos ON MATRICULAS.ALUNO_ID = alunos.ID
INNER JOIN cursos ON MATRICULAS.CURSO_ID = cursos.ID

SELECT NOME FROM alunos
SELECT CURSO

SELECT alunos.NOME AS ALUNO,CURSOS.NOME_CURSO AS CURSO FROM MATRICULAS
INNER JOIN alunos ON MATRICULAS.ALUNO_ID = alunos.ID
INNER JOIN cursos ON MATRICULAS.CURSO_ID = cursos.ID


SELECT * FROM alunos;
DELETE FROM alunos WHERE ID = 4;
